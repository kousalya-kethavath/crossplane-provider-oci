apiVersion: v1
kind: Secret
metadata:
  name: argo-workflow-manual-token
  annotations:
    # Replace this placeholder with the target ServiceAccount name before applying.
    kubernetes.io/service-account.name: replace-with-service-account-name
type: kubernetes.io/service-account-token
